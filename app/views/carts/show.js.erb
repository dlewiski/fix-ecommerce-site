$('#cart_items').html('  <% if @order_items.any? %> <% @order_items.each do |item| %> <p><%= item.product.name %> | <%= link_to "Delete", order_item_path(item), :data => {:confirm => "You sure?", :method => "delete"}, remote: true %></p> <p> Unit Price: <%= number_to_currency item.product.price %> | Quantity: <%= item.quantity %> | Total Price: <%= number_to_currency (item.product.price * item.quantity) %></p> <% end %> <p id="final_total">Your Total: <%= number_to_currency current_order.total_price %></p> <p><%= link_to "Finalize order", finalize_path %></p> <% else %> <p>Your shopping cart is empty.</p> <% end %>');
// $('#cart_items').html('  <% if @order_items.any? %>'
//     '<% @order_items.each do |item| %>'
//     '  <p><%= item.product.name %> | <%= link_to "Delete", order_item_path(item), :data => {:confirm => "You sure?", :method => "delete"}, remote: true %></p>'
//     '  <p> Unit Price: <%= number_to_currency item.product.price %> | Quantity: <%= item.quantity %> | Total Price: <%= number_to_currency (item.product.price * item.quantity) %></p>'
//   '  <% end %>'
//     '<p id="final_total">Your Total: <%= number_to_currency current_order.total_price %></p>'
//   '  <p><%= link_to "Finalize order", finalize_path %></p>'
//   '<% else %>'
//     '<p>Your shopping cart is empty.</p>'
//   '<% end %>');
